VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Optional — enables "Import from Google Photos" in the photo picker.
# Reuse the same OAuth client used for Google sign-in; it just needs
# "Authorized JavaScript origins" set and the Photos Picker scope added.
VITE_GOOGLE_CLIENT_ID=
